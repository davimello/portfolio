import * as React from 'react';
import { LoadsConfig, LoadFunction } from './types';
export declare type LoadsProps = LoadsConfig<unknown, unknown> & {
    context: string;
    children: (record: any) => React.ReactNode;
    fn: LoadFunction<unknown>;
    inputs?: Array<any>;
};
export declare const Loads: {
    ({ children, context, fn, inputs, ...config }: LoadsProps): React.ReactNode;
    defaultProps: {
        inputs: never[];
    };
};
