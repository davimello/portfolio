import { LoadFunction, LoadsConfig } from './types';
declare type ResourceOptions<Response, Err> = {
    context: string;
    [loadKey: string]: LoadFunction<Response> | [LoadFunction<Response>, LoadsConfig<Response, Err> | undefined] | string;
};
export declare function createResource<Response, Err>(opts: ResourceOptions<Response, Err>): {};
export {};
